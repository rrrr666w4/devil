# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/

# NOTE: downloads/ is intentionally NOT ignored anymore.
# It used to be listed here, which meant every downloaded video, caption,
# and metadata file was silently excluded from every commit — that was
# the main reason nothing ever showed up in the repo.

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Cookies (sensitive data)
# cookies.txt already tracked for now, but can be ignored if needed
